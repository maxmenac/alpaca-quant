// Command riskgate is the (future) deliberately-separate pre-trade risk gate. In this sprint
// it only announces its fail-closed posture.
package main

import "fmt"

func main() {
	fmt.Println("Alpaca Quant Risk Gate — fail-closed")
}
